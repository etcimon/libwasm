import std.stdio;

auto ignore(Block)(lazy Block block) {
  try {
    block();
  } catch (Exception e) {}
}

void writeIndexJsFile(string path) {
  import std.file;
  import std.algorithm;
  import std.range;
  import std.typecons;
  import std.path;
  import std.format;
  import std.conv;
  auto items = path.dirEntries("*.js", SpanMode.depth).map!(e=>tuple!("file","name")(e.name.relativePath(path),e.name.baseName.stripExtension)).filter!((i){ return i.name != "index" && i.name[0] != '.' && i.name[0] != '#';}).array;
  auto imports = items.map!((entry){return format("import * as %s from './%s.js';", entry.name, entry.name);});
  auto modulesLine = chain("const modules = [", items.map!(i => i.name).joiner(", "), "];").text;
  write(path ~ "/index.js", chain(only("// File is autogenerated with `dub run libwasm:bootstrap-webpack`"), imports, only(modulesLine), only("export { libwasm, modules }")).joiner("\n").text);
}

void main(string[] args)
{
  import std.file;
  import sdlang;
  import std.array : replace;
  immutable string libwasmjs = import("./libwasm/modules/libwasm.js");
  immutable string bindingsjs = import("./libwasm/modules/bindings.js");
  immutable string spajs = import("./libwasm/modules/spa.js");
  immutable string entryjs = import("./libwasm/entry.js");
  immutable string indexhtml = import("./libwasm/index.template.html");
  immutable string devserver = import("./dev-server.js");
  immutable string packagejson = import("./package.json");
  immutable string webpackconfigjs = import("./webpack.config.js");

  string name;
  if (exists("dub.sdl")) {
    Tag targetProject = parseFile("dub.sdl");
    name = targetProject.expectTagValue!string("name");
  } else if (exists("dub.json")) {
    import asdf;
    struct Def {
      string name;
    }
    auto project = readText("dub.json").deserialize!Def;
    name = project.name;
  } else {
    throw new Exception("Must be run in folder with dub.sdl/dub.json file");
  }
  writeln("\nThis bootstrap project sets up the js-runtime needed to run applications build on top of libwasm, as well as a webpack configuration used to bundle an `index.html`.");
  writeln("\nCopying runtime files and webpack config...");
  remove("app.js").ignore();
  mkdir("libwasm").ignore();
  mkdir("libwasm/modules").ignore();
  write("libwasm/entry.js", entryjs);
  write("libwasm/modules/libwasm.js", libwasmjs.replace("@@targetProjectName@@", name));
  writeIndexJsFile("libwasm/modules");
  write("libwasm/modules/spa.js", spajs);
  write("libwasm/modules/bindings.js", bindingsjs);
  if (exists("index.template.html"))
    rename("index.template.html","libwasm/index.template.html");
  else if (!exists("libwasm/index.template.html"))
    write("libwasm/index.template.html",indexhtml);
  write("dev-server.js",devserver.replace("@@targetProjectName@@", name));
  write("package.json",packagejson);
  write("webpack.config.js",webpackconfigjs);
  writeln("Done!");
  writeln("\nNext steps:");
  writeln("run `npm install` to install js dependencies");
  writeln("run `npx webpack` to bundle js");
  writeln("run `npm run start` to start a dev-server");
  writeln("\nNote:");
  writeln("It is assumed your wasm binary is called `"~name~"`.\nIf that is not the case, or you change it in the future, you'll need to update ./libwasm/modules/libwasm.js and ./dev-server.js\n");
}
